-- Script Kiddies Rayfield Sirius Universal Hub (Key Split Loader)
-- Discord: https://discord.gg/bbQkYU9XZe

local Players = game:GetService('Players')
local LocalPlayer = Players.LocalPlayer
local VirtualUser = game:GetService('VirtualUser')
local PlaceId = game.PlaceId

-- Key system
local userKey = "Jahceere" -- Change dynamically via GUI later
local mode = "FREE"

if userKey == "JahP" then
    mode = "PREMIUM"
    print("🔑 Script Kiddies: Premium Key Accepted. Loading premium scripts.")
elseif userKey == "Jahceere" then
    mode = "UNPAID"
    print("🔓 Script Kiddies: Unpaid user mode activated. Loading unpaid user scripts.")
else
    warn("⚠️ Invalid key provided. No scripts will load.")
end

-- Universal Anti-AFK
spawn(function()
    while true do
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new())
        task.wait(30)
    end
end)

-- Loader function for clarity
local function loadScript(url, gameName)
    local success, result = pcall(function()
        loadstring(game:HttpGet(url))()
    end)
    if success then
        print("✅ Script Kiddies: " .. gameName .. " script loaded.")
    else
        warn("❌ Script Kiddies: Failed to load " .. gameName .. " script:", result)
    end
end

-- Game-specific loaders
if PlaceId == 4581966615 then -- Anomic V4
    if mode == "PREMIUM" then
        loadScript('https://raw.githubusercontent.com/YourPremiumRepo/AnomicV4Premium/main/script.lua', "Anomic V4 (Premium)")
    elseif mode == "UNPAID" then
        loadScript('https://raw.githubusercontent.com/YourUnpaidRepo/AnomicV4Unpaid/main/script.lua', "Anomic V4 (Unpaid)")
    end

elseif PlaceId == 18668065416 then -- Blue Lock Rivals
    if mode == "PREMIUM" then
        loadScript('https://raw.githubusercontent.com/YourPremiumRepo/BlueLockRivalsPremium/main/script.lua', "Blue Lock Rivals (Premium)")
    elseif mode == "UNPAID" then
        loadScript('https://raw.githubusercontent.com/YourUnpaidRepo/BlueLockRivalsUnpaid/main/script.lua', "Blue Lock Rivals (Unpaid)")
    end

elseif PlaceId == 16472538603 then -- THA BRONX 3
    if mode == "PREMIUM" then
        loadScript('https://raw.githubusercontent.com/YourPremiumRepo/THABRONX3Premium/main/script.lua', "THA BRONX 3 (Premium)")
    elseif mode == "UNPAID" then
        loadScript('https://raw.githubusercontent.com/YourUnpaidRepo/THABRONX3Unpaid/main/script.lua', "THA BRONX 3 (Unpaid)")
    end

elseif PlaceId == 12355337193 then -- Murders vs Sheriffs
    if mode == "PREMIUM" then
        loadScript('https://raw.githubusercontent.com/YourPremiumRepo/MurdersSheriffsPremium/main/script.lua', "Murders vs Sheriffs (Premium)")
    elseif mode == "UNPAID" then
        loadScript('https://raw.githubusercontent.com/YourUnpaidRepo/MurdersSheriffsUnpaid/main/script.lua', "Murders vs Sheriffs (Unpaid)")
    end

elseif PlaceId == 2788229376 then -- Da Hood
    if mode == "PREMIUM" then
        loadScript('https://raw.githubusercontent.com/YourPremiumRepo/DaHoodPremium/main/script.lua', "Da Hood (Premium)")
    elseif mode == "UNPAID" then
        loadScript('https://raw.githubusercontent.com/YourUnpaidRepo/DaHoodUnpaid/main/script.lua', "Da Hood (Unpaid)")
    end

elseif PlaceId == 16498193900 then -- Lootify
    if mode == "PREMIUM" then
        loadScript('https://raw.githubusercontent.com/YourPremiumRepo/LootifyPremium/main/script.lua', "Lootify (Premium)")
    elseif mode == "UNPAID" then
        loadScript('https://raw.githubusercontent.com/YourUnpaidRepo/LootifyUnpaid/main/script.lua', "Lootify (Unpaid)")
    end
end

print("✅ Script Kiddies loader completed. Mode:", mode)
